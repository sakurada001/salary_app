<!DOCTYPE html>
<html>
  <head>
  <% if logged_in? %>
  <meta name="current-user-id" content="<%= current_user.id %>">
  <meta name="default-travel-cost" content="<%= current_user.transportation_fee || 0 %>">
<% end %>
  <% if logged_in? %>
  <meta name="current-user-id" content="<%= current_user.id %>">
  <% end %>
    <title><%= content_for(:title) || "Salary App" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Salary App">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# 1. 外部CSSの読み込み %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <%# 2. アイコンの設定 %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# 3. Railsのアセット読み込み（JSとCSS） %>
    <%= stylesheet_link_tag :application, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
    <%= yield :head %>
  </head>

  <body>
    <%# フラッシュメッセージは body の一番上に移動させます %>
    <div class="container mt-3">
      <% flash.each do |message_type, message| %>
        <div class="alert alert-<%= message_type == 'success' ? 'success' : 'danger' %> alert-dismissible fade show" role="alert">
          <%= message %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>
    </div>

    <%# 各ページのメインコンテンツ %>
    <%= yield %>

    <%# Bootstrap の JavaScript (必要であれば) %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>