<div class="container mt-5">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h2>ユーザー一覧</h2>
    <%= link_to "+ 従業員を登録", signup_path, class: "btn btn-success" %>
  </div>

  <div class="card shadow-sm">
    <div class="card-body">
      <% if @users.any? %>
        <table class="table table-hover align-middle">
          <thead class="table-light">
            <tr>
              <th>ID</th>
              <th>氏名</th>
              <th>メールアドレス</th>
              <th>時給</th>
              <th>登録日時</th>
            </tr>
          </thead>
          <tbody>
            <% @users.each do |user| %>
              <tr>
                <td><%= user.id %></td>
                <td><%= user.name %></td>
                <td><%= user.email %></td>
                <td><%= user.hourly_wage.to_s(:delimited) %> 円</td>
                <td><%= user.created_at.strftime('%Y/%m/%d %H:%M') %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      <% else %>
        <%# データがない時はこれを表示 %>
        <div class="text-center text-muted py-5">
          <p>まだ登録されているユーザーがいません。</p>
          <p>右上のボタンから登録してください。</p>
        </div>
      <% end %>
    </div>
  </div>

  <div class="mt-4 text-center">
    <%= link_to "ホームに戻る", home_path %>
  </div>
</div>